// include "views-core", "views-json"
include 'core', 'json', 'markup', 'gradle', 'functional-tests','functional-tests-plugin', 'docs', 'json-templates', 'json-testing-support'
rootProject.children.each { it.name = "views-${it.name}" }