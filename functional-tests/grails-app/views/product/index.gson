import functional.tests.Product

model {
	Iterable<Product> productList
	Integer productCount
	Integer max
	Integer offset
	String sort
	String order
}

response.contentType "application/hal+json"

json {
	hal.paginate(Product, productCount, max, offset, sort, order)
	_embedded {
		products tmpl.product(productList ?: [])
	}
	count productCount
	max max
	offset offset
	sort sort
	order order
}
